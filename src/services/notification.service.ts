import { Injectable } from "@angular/core";
import { Policeman } from "src/models/policeman.model";

@Injectable({providedIn: 'root'})

export class NotificationService {
    notifyPolicemans(policemans: Policeman[]) {
        
    }
}